import Weather from "./Components/Weather/Weather";

const Home = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
        width: "100vw",
      }}
    >
      <Weather />
    </div>
  );
};

export default Home;
